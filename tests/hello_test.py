import pytest
import find_substrings import find_substrings

def hello_test(string):
    string='hello'
    substring.find_substrings(string)
    assert(substring.list == ['h', 'he', 'hel', 'hell', 'hello', 'e', 'el', 'ell', 'ello', 'l', 'll', 'llo', 'l', 'lo', 'o'])
    assert(substring.count == 15)
